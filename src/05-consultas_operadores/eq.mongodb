use("platzi_store")

// Encuentra todos los documentos en la colección inventory donde el valor del campo qty es exactamente igual a 20.
db.inventory.find({ qty: { $eq: 20 } });

//Es lo mismo que el anterior sin usar el operador
//db.inventory.find({ qty: 20 });

